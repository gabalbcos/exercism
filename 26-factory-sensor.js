export function checkHumidityLevel(humidity) {
  if (humidity > 70) {
    throw new Error ('Oh, no... Humidity too high, no good!')
  }
}

//instances of error
//Broken Sensor
class ArgumentError extends Erro{}

function reportOverheating(temperature) {
try {
  
} catch(error) {
  
}
}